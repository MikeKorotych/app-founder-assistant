# .env.local — Next.js web surface local environment.
# Copy to .env.local (gitignored) and fill in real values. In production,
# GitHub Actions injects these during the OpenNext build from repo secrets.

# hahaton-api Worker URL (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:8787
