{
  "name": "@hahaton/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drizzle schema + D1 client for run persistence. Migrations live here and are applied by the Worker.",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "drizzle-kit generate",
    "clean": "rimraf .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@hahaton/contracts": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "drizzle-kit": "catalog:",
    "rimraf": "^6.0.1",
    "typescript": "catalog:"
  }
}
