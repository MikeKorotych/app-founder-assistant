{
  "name": "@hahaton/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frozen shared contract — the single source of truth every workspace builds against.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rimraf .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "catalog:"
  }
}
