/**
 * @hahaton/contracts — the frozen shared contract.
 *
 * Re-exports every domain shape from `types.ts`. Apps and packages import from
 * the package root (`@hahaton/contracts`), never reach into `dist/` directly.
 */
export * from "./types.js";
