# Strict by default: workspace packages must declare every dependency they import.
# Flip to true only if a transitive dep needs to be hoisted for a tool that
# can't see pnpm's nested node_modules.
shamefully-hoist=false

# Always link workspace packages from source instead of the registry.
link-workspace-packages=true
prefer-workspace-packages=true
